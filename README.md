*This project has been created as part of the 42 curriculum by ksener.*

# Libft - 42 Kocaeli 🇹🇷

## 📝 Description
Libft is the foundational project of the 42 curriculum. It consists of a custom C library containing a collection of general-purpose functions that mimic the standard C library (libc) and additional utilities.

## 📂 Library Content

### Part 1: Libc Functions
`ft_isalpha`, `ft_isdigit`, `ft_isalnum`, `ft_isascii`, `ft_isprint`, `ft_strlen`, `ft_memset`, `ft_bzero`, `ft_memcpy`, `ft_memmove`, `ft_strlcpy`, `ft_strlcat`, `ft_toupper`, `ft_tolower`, `ft_strchr`, `ft_strrchr`, `ft_strncmp`, `ft_memchr`, `ft_memcmp`, `ft_strnstr`, `ft_atoi`, `ft_calloc`, `ft_strdup`

### Part 2: Additional Functions
`ft_substr`, `ft_strjoin`, `ft_strtrim`, `ft_split`, `ft_itoa`, `ft_strmapi`, `ft_striteri`, `ft_putchar_fd`, `ft_putstr_fd`, `ft_putendl_fd`, `ft_putnbr_fd`

### Part 3: Linked Lists
`ft_lstnew`, `ft_lstadd_front`, `ft_lstsize`, `ft_lstlast`, `ft_lstadd_back`, `ft_lstdelone`, `ft_lstclear`, `ft_lstiter`, `ft_lstmap`

## 🛠️ Instructions
To compile the library, use the following rules in the Makefile:

| Command       | Action                            |
| :---          | :---                              |
| `make`        | Compiles the library `libft.a`    |
| `make clean`  | Removes object files              |
| `make fclean` | Removes object files and `libft.a`|
| `make re`     | Performs a full re-compile        |

## 📚 Resources & AI Use
- **Resources:** Standard C library man pages, 42 Norm documentation.
- **AI Use:** AI was used for logic discussion, Makefile help, and documentation. No code was generated by AI. It only provided guidance to follow 42 rules and project requirements.
